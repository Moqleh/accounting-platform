'use client';
import { FormEvent, useEffect, useMemo, useState } from 'react';
import { ErpShell, useLocale } from '@/components/erp-shell';
import { apiGet, apiPost, DEMO_COMPANY_ID } from '@/lib/api';

type Customer={id:string;name:string};type Supplier={id:string;name:string};type Journal={id:string;journalNumber:string;sourceType:string;transactionDate:string;status:string;lines:Array<{baseDebit:string;baseCredit:string}>};
export default function Page(){return <ErpShell><Payments/></ErpShell>}
function Payments(){const {locale}=useLocale();const ar=locale==='ar';const [mode,setMode]=useState<'customer'|'supplier'>('customer');const [customers,setCustomers]=useState<Customer[]>([]),[suppliers,setSuppliers]=useState<Supplier[]>([]),[journals,setJournals]=useState<Journal[]>([]);const [partyId,setPartyId]=useState(''),[amount,setAmount]=useState(''),[date,setDate]=useState('2026-09-11'),[reference,setReference]=useState('');const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);const load=()=>Promise.all([apiGet<Customer[]>(`/erp/customers/${DEMO_COMPANY_ID}`),apiGet<Supplier[]>(`/erp/suppliers/${DEMO_COMPANY_ID}`),apiGet<Journal[]>(`/erp/journals/${DEMO_COMPANY_ID}`)]).then(([c,s,j])=>{setCustomers(c);setSuppliers(s);setJournals(j);setPartyId((mode==='customer'?c[0]?.id:s[0]?.id)??'')}).catch(e=>setMessage(e instanceof Error?e.message:String(e)));useEffect(()=>{load()},[]);useEffect(()=>{setPartyId((mode==='customer'?customers[0]?.id:suppliers[0]?.id)??'')},[mode,customers,suppliers]);const history=useMemo(()=>journals.filter(j=>j.sourceType==='CustomerPayment'||j.sourceType==='SupplierPayment'),[journals]);const submit=async(e:FormEvent)=>{e.preventDefault();if(!partyId||!amount)return;setBusy(true);setMessage('');try{if(mode==='customer')await apiPost('/payments/customer-receipt',{companyId:DEMO_COMPANY_ID,customerId:partyId,amount,date:`${date}T12:00:00Z`,currencyCode:'SAR',exchangeRate:'1',reference});else await apiPost('/payments/supplier-payment',{companyId:DEMO_COMPANY_ID,supplierId:partyId,amount,date:`${date}T12:00:00Z`,currencyCode:'SAR',exchangeRate:'1',reference});setAmount('');setReference('');setMessage(ar?'تم ترحيل الحركة':'Transaction posted');await load()}catch(err){setMessage(err instanceof Error?err.message:String(err))}finally{setBusy(false)}};return <><div className="heading"><div><h1>{ar?'الصندوق والبنوك':'Cash & Banks'}</h1><p>{ar?'سندات القبض والصرف والحسابات البنكية':'Receipts, payments and bank accounts'}</p></div></div><section className="gridTwo"><form className="card" onSubmit={submit}><div className="cardTitle"><h2>{mode==='customer'?(ar?'سند قبض عميل':'Customer Receipt'):(ar?'دفعة مورد':'Supplier Payment')}</h2></div><div className="segmented"><button type="button" className={mode==='customer'?'primary':'ghost'} onClick={()=>setMode('customer')}>{ar?'قبض':'Receipt'}</button><button type="button" className={mode==='supplier'?'primary':'ghost'} onClick={()=>setMode('supplier')}>{ar?'صرف':'Payment'}</button></div><label>{mode==='customer'?(ar?'العميل':'Customer'):(ar?'المورد':'Supplier')}<select value={partyId} onChange={e=>setPartyId(e.target.value)}>{(mode==='customer'?customers:suppliers).map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>{ar?'المبلغ':'Amount'}<input required value={amount} onChange={e=>setAmount(e.target.value)} inputMode="decimal"/></label><label>{ar?'التاريخ':'Date'}<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>{ar?'المرجع':'Reference'}<input value={reference} onChange={e=>setReference(e.target.value)} placeholder={ar?'اختياري':'Optional'}/></label>{message&&<div className="formMessage">{message}</div>}<button className="primary" disabled={busy}>{busy?(ar?'جاري الترحيل...':'Posting...'):(ar?'حفظ وترحيل':'Save & Post')}</button></form><div className="card"><div className="cardTitle"><h2>{ar?'آخر الحركات':'Recent Transactions'}</h2></div><div className="tableWrap"><table><thead><tr><th>{ar?'رقم القيد':'Journal #'}</th><th>{ar?'النوع':'Type'}</th><th>{ar?'التاريخ':'Date'}</th><th>{ar?'المبلغ':'Amount'}</th></tr></thead><tbody>{history.slice(0,10).map(j=>{const total=j.lines.reduce((a,l)=>a+Number(l.baseDebit),0);return <tr key={j.id}><td>{j.journalNumber}</td><td>{j.sourceType}</td><td>{j.transactionDate.slice(0,10)}</td><td>{total.toFixed(2)}</td></tr>})}</tbody></table></div></div></section></>}
