# تحديث مراجعة توازن القيود في المعاينة

هذه متابعة لتقرير الفحص السابق، وتخص إصلاحات `preview-api.ts` المتفق عليها فقط. بُني التعديل على `9218aae` ويحافظ على التحديث السابق في `api.ts`.

## التغييرات المنفذة

- تحقق مركزي وصرف في `validateJournalLines` من الحساب النشط، ونوع المبلغ وصلاحيته، وعدم السلبية أو ازدواج المدين والدائن أو السطور الصفرية. قبول التوازن فقط عندما يكون الفرق المطلق أقل من `1e-6`، مع رفض تجاوز المجاميع للحد العددي.
- تحقق مسبق في مسارات القيود والمبيعات والمشتريات والمدفوعات، وإعادة التحقق داخل `journal()` في كل بناء.
- حساب تخصيصات FIFO دون تعديل المخزون، ومنع الأصناف المكررة، وفحص المستودع ونشاطه، واختيار دفعاته حسب تاريخ الاستلام ثم المعرّف. الخدمات لا تستهلك المخزون.
- توحيد معرّف المصدر بين المستند والقيد؛ مشتريات الخدمات تستخدم حساب الشراء الخاص بالصنف بدل حساب المخزون.
- تمرير المدخلات الخام للتحقق؛ رفض boolean والمصفوفات والكائنات وNaN وInfinity والنصوص الفارغة.
- رفض إقفال السنة صراحةً في المعاينة دون قيد أو تدقيق أو كتابة، حتى قبل تهيئة البيانات.
- قراءة بيانات المعاينة لا تكتب بذاتها؛ الطلبات المرفوضة لا تنشئ تخزينًا أوليًا. البيانات التالفة لا تُستبدل تلقائيًا.
- إعداد TypeScript مستقل للاختبارات، وتشغيله داخل سكربت test قبل اختبارات Node.

## التحقق

اختبارات الواجهة تشمل نقاء دالة التحقق، ونجاح القيود والمدفوعات والفواتير، وحد السماحية بإشارتيه، و0.1 + 0.2، وتجاوز المجاميع، والمستودعات، وترتيب FIFO، والخدمات، وتطابق معرّف المصدر. اختبارات الرفض تقارن التخزين كاملًا وتراقب عدد عمليات الكتابة. يُستعاد وصف المتغيرات العامة بعد كل اختبار.

ثبات التخزين لا يُقدَّم كإثبات لعدم تعديل كائن مؤقت في الذاكرة؛ فحص النقاء مستقل، وتخطيط FIFO يُنفَّذ قبل تطبيق تعديلاته.

التحقق المحلي: 97 اختبارًا ناجحًا، وفحص TypeScript وبناء Next.js ناجحان. شُغلت سكربتات package.json بواسطة npm بسبب توقف مشغّل pnpm المحلي عند سكربتات تثبيت اعتماديات Backend؛ لم تُغيّر سياسة السماح لتلك السكربتات. الاختبارات تترجم TypeScript بواسطة tsc فعلًا، دون محاكاة مصدره أو استبداله.

## الحدود المتفق عليها

هذا الإصلاح لا يعني اكتمال تدقيق النظام المحاسبي كله. بقيت محاسبة المرتجعات في المعاينة وسياسة التقريب وFX ونسبة ضريبة المعاينة الثابتة خارج هذا التعديل. رفض الإقفال مقصود إلى حين تنفيذ إقفال كامل. إعدادات Backend وDocker وصلاحيات PostgreSQL من التقرير السابق ليست ضمن هذا الإصلاح. لا يُعلن نشر الموقع أو نجاح CI قبل تأكيدهما من GitHub.

## حالة الدمج والنشر — 2026-09-12

تم إصلاح البنود المتفق عليها ودمج PR #1 في main بالنسخة `5b22ecbb80aecc2ea8b5b66fc5fe22c5f1bff4f5` ونشرها على GitHub Pages.

- CI بعد الدمج ناجح: https://github.com/Moqleh/accounting-platform/actions/runs/34710326920
- Workflow إعداد النشر ناجح: https://github.com/Moqleh/accounting-platform/actions/runs/34710326866
- نشر Pages ناجح: https://github.com/Moqleh/accounting-platform/actions/runs/34710371015
- الصفحة الحية أعادت HTTP 200 وطابقت blob `122a986a9daeed44381388a5bb0adfe43f45bfb1` من نسخة النشر `e5c0e9a886e6ec3f12046b512b6dbc058c4477be`.
- حزمة الموقع `315-5607ff27d228249c.js` أعادت HTTP 200 وطابقت blob `aa705f065cdfb99a2c2f622a9c6b052c2ec03d3f`.
- اختُبر كود الحزمة المنزلة من الموقع مباشرة داخل Node VM بمحاكاة localStorage: رفض القيد غير المتوازن، وquantity:true، والمستودع غير الموجود دون أي كتابة؛ وقَبِل فاتورة صحيحة. هذا فحص لسلوك الحزمة المنشورة، وليس اختبار نقرات في متصفح.
- سجل قبول API في CI الخاص بالـPR أثبت إنشاء مستندات فعلية وميزان مراجعة `trialDebit = trialCredit = 502.5`.
- فُتحت متابعة ترقية Actions: https://github.com/Moqleh/accounting-platform/issues/2

هذه الحالة تخص إصلاحات المعاينة المراجعة فقط؛ تبقى البنود الخارجة عن النطاق المذكورة أعلاه مستقلة.
